{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build active C++ file",
			"type": "shell",
			"command": "g++",
			"args": [
				"-g",
				"-o",
				"${fileDirname}/${fileBasenameNoExtension}",
				"${file}"
			],
			"problemMatcher": [
				"$gcc"
			],
			"group": "build"
		}
	]
}
